# Makefile

clean:
	rm -f ".o files" core ".exe/class files" "outputs"
	rm -f *.*~
	rm -f *~
	rm -f *.out
	rm -f MSS_Results.txt
	ls

stutest.out:

	python MaxSubArray.py MSS_Problems.txt > MSS_Results.txt
	cat MSS_Results.txt
